/**
 * Adapter Supabase — belum diisi.
 *
 * TODO(produksi): prototype ini berjalan di atas `localRepo`. Saat backend
 * nyata dipasang, implementasikan `DataRepo` di sini (atau langsung
 * `LaravelApiRepo`) dan daftarkan di `lib/data/index.ts`. Tidak ada kode UI
 * yang perlu berubah karena seluruh call site memakai kontrak yang sama.
 *
 * Catatan pemetaan yang sudah pasti:
 * - `Database` di `lib/domain/types.ts` = daftar tabel.
 * - Aturan di `lib/domain/status.ts` dan `stok.ts` harus dijalankan di sisi
 *   server, bukan dipercayakan ke klien.
 * - Stok tetap dihitung dari riwayat, jangan disimpan sebagai kolom angka.
 */

export {}
